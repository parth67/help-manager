const state = {
  data: []
}

const mutations = {
  createNewFile (state, payload) {
    // let {fileName} = payload
  },
  addReadedFile (state, payload) {
    // let {fileName, filePath, json} = payload
  }
}

const actions = {}

export default {
  state,
  mutations,
  actions
}
